apiVersion: syndesis.io/v1beta1
kind: Syndesis
metadata:
  name: app
spec:
  components:
    meta:
      resources:
        volumeCapacity: "215Mi"
    prometheus:
      resources:
        volumeCapacity: "999Mi"
