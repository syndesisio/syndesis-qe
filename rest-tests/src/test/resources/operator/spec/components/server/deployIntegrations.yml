apiVersion: syndesis.io/v1beta1
kind: Syndesis
metadata:
  name: app
spec:
  components:
    server:
      features:
        deployIntegrations: false
        mavenRepositories:
          nexus: "REPLACE_REPO"
