apiVersion: syndesis.io/v1beta3
kind: Syndesis
metadata:
  name: app
spec:
  addons:
    todo:
      enabled: true
  components:
    server:
      features:
        integrationLimit: 1
        integrationStateCheckInterval: 60
  devSupport: false
