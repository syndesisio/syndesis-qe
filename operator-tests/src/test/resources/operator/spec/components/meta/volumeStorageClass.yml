apiVersion: syndesis.io/v1beta3
kind: Syndesis
metadata:
  name: app
spec:
  components:
    database:
      resources:
        volumeCapacity: 5Gi
    meta:
      resources:
        volumeStorageClass: filesystem
    prometheus:
      resources:
        volumeCapacity: 5Gi
