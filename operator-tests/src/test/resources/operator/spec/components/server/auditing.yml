apiVersion: syndesis.io/v1beta3
kind: Syndesis
metadata:
  name: app
spec:
  demoData: true
  components:
    server:
      features:
        auditing: true
        maven:
          append: true
          repositories:
            nexus: "REPLACE_REPO"
