apiVersion: syndesis.io/v1beta2
kind: Syndesis
metadata:
  name: app
spec:
  components:
    database:
      resources:
        volumeCapacity: 5Gi
    meta:
      resources:
        volumeCapacity: 5Gi
    prometheus:
      resources:
        volumeCapacity: 3Gi
